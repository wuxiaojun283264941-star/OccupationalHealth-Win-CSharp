[服务器配置]
; ============================================
; 职业健康体检管理平台 - 客户端服务器配置
; ============================================
; 修改下面的 IP 和端口来连接不同的服务器
; 部署到远程服务器时，将 localhost 改为实际 IP
; ============================================

; 服务器 IP 地址或域名（局域网填内网IP，公网填公网IP或域名）
IP地址=localhost

; 服务端口号（默认 3001）
端口=3001

; 协议（http 或 https）
协议=http
